import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pt-mate-therapist-2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
